# Q 语言教程 - 基本类型

## 目录

1. [整数类型](#整数类型)
2. [浮点类型](#浮点类型)
3. [布尔类型](#布尔类型)
4. [字符和字符串](#字符和字符串)
5. [数组](#数组)
6. [切片](#切片)
7. [Map](#map)
8. [特殊类型](#特殊类型)
9. [类型转换](#类型转换)
10. [类型推导](#类型推导)

---

## 整数类型

Q 语言提供了丰富的整数类型，支持有符号和无符号整数。

### 有符号整数

```q
// 平台相关整数（32位系统为i32，64位系统为i64）
var a: int = 100
var b: int = -50

// 固定大小的有符号整数
var c1: i8 = 127        // 8位：-128 到 127
var c2: i16 = 32767     // 16位：-32768 到 32767
var c3: i32 = 2147483647    // 32位
var c4: i64 = 9223372036854775807  // 64位
```

### 无符号整数

```q
// 平台相关无符号整数
var x: uint = 100

// 固定大小的无符号整数
var y1: u8 = 255        // 8位：0 到 255
var y2: u16 = 65535     // 16位：0 到 65535
var y3: u32 = 4294967295    // 32位
var y4: u64 = 18446744073709551615  // 64位

// byte 是 u8 的别名
var z: byte = 255
```

### 整数字面量

```q
var decimal = 42        // 十进制
var hex = 0x2A         // 十六进制（未实现）
var binary = 0b101010  // 二进制（未实现）
```

### 整数运算

```q
var a = 10
var b = 3

var sum = a + b        // 加法：13
var diff = a - b       // 减法：7
var prod = a * b       // 乘法：30
var quot = a / b       // 除法：3（整数除法）
var rem = a % b        // 取模：1

// 复合赋值
var c = 5
c += 2    // c = 7
c -= 1    // c = 6
c *= 3    // c = 18
c /= 2    // c = 9
c %= 4    // c = 1
```

---

## 浮点类型

Q 语言支持两种精度的浮点数。

### 基本使用

```q
// 32位单精度浮点数
var x: f32 = 3.14

// 64位双精度浮点数（默认）
var y: f64 = 3.141592653589793
var z = 2.718  // 类型推导为 f64
```

### 浮点字面量

```q
var a = 3.14           // 普通小数
var b = 1.0e10         // 科学记数法（未实现）
var c = -0.5           // 负数
```

### 浮点运算

```q
var a = 10.5
var b = 3.2

var sum = a + b        // 13.7
var diff = a - b       // 7.3
var prod = a * b       // 33.6
var quot = a / b       // 3.28125

// 复合赋值
var c = 5.0
c += 2.5   // 7.5
c *= 2.0   // 15.0
```

---

## 布尔类型

布尔类型只有两个值：`true` 和 `false`。

### 基本使用

```q
var isTrue: bool = true
var isFalse: bool = false

// 类型推导
var flag = true  // 推导为 bool
```

### 比较运算

```q
var a = 10
var b = 20

var eq = a == b    // false（等于）
var ne = a != b    // true（不等于）
var lt = a < b     // true（小于）
var le = a <= b    // true（小于等于）
var gt = a > b     // false（大于）
var ge = a >= b    // false（大于等于）
```

### 逻辑运算

```q
var a = true
var b = false

var and = a && b   // false（逻辑与）
var or = a || b    // true（逻辑或）
var not = !a       // false（逻辑非）

// 短路求值
var x = 10
var result = (x > 0) && (100 / x > 5)  // 先计算 x > 0，为真才计算后面
```

---

## 字符和字符串

### 字符类型

字符类型 `char` 表示 Unicode 码点（4字节）。

```q
var ch: char = 'A'
var zh: char = '中'
var emoji: char = '😊'  // Unicode 字符
```

### 字符串类型

字符串是不可变的引用类型。

```q
var s1: string = "Hello"
var s2: string = "世界"

// 类型推导
var greeting = "Hello, World!"
```

### 字符串拼接

```q
var first = "Hello"
var last = "World"
var full = first + ", " + last + "!"  // "Hello, World!"
```

### 字符串操作

目前 Q 语言的字符串操作比较基础，主要通过内置函数和运算符：

```q
var s = "Hello"

// 字符串拼接
var s2 = s + " World"   // "Hello World"

// 字符串比较
var eq = s == "Hello"   // true
var ne = s != "World"   // true
```

**注意**：字符串方法（如 `length()`、`substring()` 等）在设计文档中定义，但当前版本可能尚未实现。

---

## 数组

数组是固定长度的值类型，元素存储在连续内存中。

### 数组声明

```q
// 声明并初始化
var numbers: int[5] = [1, 2, 3, 4, 5]
var names: string[3] = ["Alice", "Bob", "Charlie"]

// 类型推导（推导为切片，见下文）
var items = [1, 2, 3]
```

### 数组访问

```q
var arr = [10, 20, 30, 40, 50]

// 读取元素
var first = arr[0]   // 10
var last = arr[4]    // 50

// 修改元素
arr[2] = 99
println(arr[2])      // 99
```

---

## 切片

切片是动态长度的引用类型，是对底层数组的引用。

### 切片创建

```q
// 直接初始化
var slice1: int[] = [1, 2, 3, 4, 5]

// 空切片
var slice2: int[] = []

// 类型推导
var slice3 = [1, 2, 3]  // 推导为 int[]
```

### 切片操作

```q
var nums = [1, 2, 3, 4, 5]

// 访问元素
var first = nums[0]    // 1
var last = nums[4]     // 5

// 修改元素
nums[0] = 10
println(nums[0])       // 10
```

**注意**：切片的高级操作（如 `append()`、`insert()` 等方法）在设计文档中定义，但当前版本可能尚未完全实现。

### 数组 vs 切片

```q
// 数组：固定长度，值类型
var arr: int[3] = [1, 2, 3]

// 切片：动态长度，引用类型
var slice: int[] = [1, 2, 3]
```

---

## Map

Map 是键值对的集合，是引用类型。

### Map 创建

```q
// 空 Map
var ages: map[string]int = {}

// 初始化 Map
var scores = {
    "Alice": 95,
    "Bob": 87,
    "Charlie": 92
}

// 类型推导
var config = {
    "host": "localhost",
    "port": "8080"
}  // 推导为 map[string]string
```

### Map 操作

```q
var ages = {
    "Alice": 25,
    "Bob": 30
}

// 添加/修改元素
ages["Charlie"] = 28

// 访问元素
var age = ages["Alice"]   // 25
println(age)
```

**注意**：Map 的方法（如 `get()`、`has()`、`remove()` 等）在设计文档中定义，但当前版本可能尚未完全实现。

---

## 特殊类型

### Void 类型

`void` 表示无返回值的函数。

```q
func printMessage(msg: string) void {
    println(msg)
    // 无返回值
}

// 返回类型可以省略，默认为 void
func sayHello() {
    println("Hello!")
}
```

### Null 类型

`null` 表示空值，只能赋给可空类型。

```q
// 可空类型（在类型后加 ?）
var name: string? = null
var age: int? = null

// 赋值
name = "Alice"
age = 25
```

### Unknown 类型

`unknown` 是安全的顶类型，可以接收任何值，但使用前必须进行类型检查。

```q
var value: unknown = 42
value = "Hello"
value = true

// 使用前需要类型检查或转换
```

### Dynamic 类型

`dynamic` 跳过编译时类型检查，在运行时动态解析类型。谨慎使用！

```q
var d: dynamic = 42
d = "Hello"  // 允许
d = true     // 允许
```

---

## 类型转换

### 显式类型转换

使用 `as` 关键字进行类型转换：

```q
// 基本类型转换
var a: i32 = 100
var b: i64 = a as i64      // 安全转换（扩展）

var x: f64 = 3.14
var y: f32 = x as f32      // 缩小转换

// 整数与浮点数转换
var i = 10
var f = i as f64           // 10.0

var f2 = 3.14
var i2 = f2 as int         // 3（截断）
```

### 类型检查

使用 `is` 关键字检查类型：

```q
var value: unknown = 42

if value is int {
    println("It's an integer")
}
```

---

## 类型推导

Q 语言支持类型推导，编译器可以根据初始值自动推断变量类型。

### 基本类型推导

```q
var i = 42              // 推导为 int
var f = 3.14            // 推导为 f64
var b = true            // 推导为 bool
var s = "Hello"         // 推导为 string
```

### 集合类型推导

```q
var nums = [1, 2, 3]           // 推导为 int[]
var names = ["Alice", "Bob"]    // 推导为 string[]
var config = {                  // 推导为 map[string]string
    "key": "value"
}
```

### 显式类型注解

虽然支持类型推导，但在某些情况下显式指定类型更清晰：

```q
// 推荐：显式指定类型
var numbers: int[] = [1, 2, 3]
var age: int = 25

// 也可以：类型推导
var numbers2 = [1, 2, 3]
var age2 = 25
```

---

## 类型总结

### 值类型（栈分配）

- 所有整数类型：`int`、`i8`、`i16`、`i32`、`i64`、`uint`、`u8`、`u16`、`u32`、`u64`、`byte`
- 所有浮点类型：`f32`、`f64`
- 布尔类型：`bool`
- 字符类型：`char`
- 数组类型：`T[N]`
- 结构体类型：`struct`

### 引用类型（堆分配）

- 字符串类型：`string`
- 切片类型：`T[]`
- Map 类型：`map[K]V`
- 类类型：`class`
- 接口类型：`interface`

### 类型大小

```q
// sizeof 内置函数可以获取类型大小（字节）
var size1 = sizeof(int)     // 平台相关（4或8）
var size2 = sizeof(i32)     // 4
var size3 = sizeof(f64)     // 8
var size4 = sizeof(bool)    // 1
```

### 类型信息

```q
// typeof 内置函数可以获取类型信息（用于反射）
var t1 = typeof(42)         // int 类型信息
var t2 = typeof("Hello")    // string 类型信息
```

---

## 最佳实践

1. **明确类型注解**：对于公共 API，建议显式指定类型，提高代码可读性
2. **选择合适的整数类型**：大多数情况下使用 `int`，需要特定大小时才使用 `i32`、`i64` 等
3. **浮点数默认 f64**：除非内存敏感，否则使用 `f64`
4. **避免使用 dynamic**：只在必要时使用，优先使用 `unknown` 并进行类型检查
5. **字符串不可变**：任何"修改"操作都会创建新字符串，注意性能

---

## 下一步

- 学习 [控制结构](./控制结构.md)
- 学习 [结构体](./结构体.md)  
- 学习 [面向对象](./面向对象.md)
