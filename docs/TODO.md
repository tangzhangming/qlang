# Q语言待完成项目清单

## ✅ 已完成项目

| 模块 | 功能 | 描述 | 完成日期 |
|------|------|------|----------|
| 类型系统 | 可空类型 `?` | `int?` 表示可空整数，支持 `null` 赋值 | 2026-01-17 |
| 类型系统 | 安全调用 `?.` | 空值安全的成员访问 `obj?.field` | 2026-01-17 |
| 类型系统 | 非空断言 `!.` | 断言非空后访问 `obj!.field` | 2026-01-17 |
| 类型系统 | 空值合并 `??` | 默认值运算符 `a ?? b` | 2026-01-17 |
| 类型系统 | 类型别名 | `type ID = int` 定义类型别名 | 2026-01-17 |
| 字符串 | 多行字符串 | 三引号 `"""..."""` 多行字符串 | 2026-01-17 |
| 字符串 | 原样字符串 | 单引号 `'...'` 不转义字符串 | 2026-01-17 |
| 控制流 | 带标签循环 | `outer: for { break outer }` | 2026-01-17 |
| 内置类型 | Array 方法 | `.push()`, `.pop()`, `.len()`, `.first()`, `.last()`, `.contains()`, `.reverse()`, `.clear()`, `.indexOf()`, `.join()`, `.slice()`, `.concat()`, `.copy()`, `.isEmpty()` | 2026-01-17 |
| 内置类型 | Array 高阶方法 | `.collect()`, `.filter()`, `.reduce()`, `.forEach()`, `.find()`, `.findIndex()`, `.every()`, `.some()`, `.sort()` | 2026-01-17 |
| 内置类型 | String 方法 | `.len()`, `.split()`, `.trim()`, `.replace()`, `.contains()`, `.startsWith()`, `.endsWith()`, `.toUpper()`, `.toLower()`, `.charAt()`, `.indexOf()`, `.substring()`, `.repeat()`, `.isEmpty()`, `.reverse()` | 2026-01-17 |
| 内置类型 | Map 方法 | `.len()`, `.keys()`, `.values()`, `.has()`, `.get()`, `.set()`, `.remove()`, `.clear()`, `.isEmpty()` | 2026-01-17 |
| 内置类型 | Range 方法 | `.start()`, `.end()`, `.len()`, `.contains()`, `.toArray()`, `.isInclusive()`, `.isEmpty()`, `.step()` | 2026-01-17 |
| OOP | 静态方法调用 | `ClassName.method()` 静态方法访问 | 2026-01-17 |
| OOP | 抽象类 | `abstract class` 抽象类定义和继承 | 2026-01-17 |
| OOP | 运算符重载 | `operator_add(other)` 重载运算符 | 2026-01-17 |
| OOP | Trait 默认方法 | `use Trait` 继承 trait 默认方法 | 2026-01-17 |
| OOP | 继承方法查找 | 子类沿继承链查找父类方法 | 2026-01-17 |
| 数据结构 | 固定数组 | `int[10]` 固定长度数组类型 | 2026-01-17 |
| 数据结构 | 切片类型 | `int[]` 动态切片类型 | 2026-01-17 |
| 异常处理 | try/catch/throw | `try { } catch(e) { } finally { }` 异常处理 | 2026-01-17 |

## 🐛 已知问题

| 模块 | 问题 | 描述 |
|------|------|------|
| OOP | 多类同文件方法表冲突 | 同一文件中定义多个类时，后定义类的方法可能覆盖先定义类的方法表 |
| 闭包 | 变量捕获不完整 | 闭包无法捕获外部作用域的变量进行修改 |
| 语法 | `map` 关键字冲突 | `map` 是 Map 类型关键字，数组 map 方法需用 `collect()` 代替 |

## 📋 待完成项目

| 优先级 | 模块 | 功能 | 描述 | 复杂度 |
|--------|------|------|------|--------|
| P1 | 类型系统 | 泛型基础 | `class List<T>` 泛型类和函数 | 高 |
| P2 | OOP | super 表达式 | `super.method()` 调用父类方法 | 中 |
| P3 | 模块系统 | package 声明 | `package mylib` 包声明 | 中 |
| P3 | 模块系统 | import 导入 | `import "path/to/module"` | 高 |
| P3 | 并发 | go 协程 | `go fn()` 启动协程 | 高 |
| P3 | 并发 | channel | 协程间通信通道 | 高 |
| P4 | 工具 | REPL | 交互式命令行环境 | 中 |
| P4 | 工具 | 调试器 | 断点、单步执行支持 | 高 |
| P4 | 工具 | 格式化工具 | 代码自动格式化 | 中 |
| P4 | 优化 | GC 实现 | 垃圾回收器 | 高 |
| P4 | 优化 | 字节码优化 | 常量折叠、死代码消除 | 高 |

---

## 优先级说明

- **P1**: 重要功能，提升语言表达能力
- **P2**: 增强功能，完善 OOP 和控制流
- **P3**: 高级功能，模块化和并发支持（暂缓）
- **P4**: 工具和优化，提升开发体验

## 复杂度说明

- **低**: 1-2 天可完成
- **中**: 3-5 天可完成
- **高**: 1-2 周可完成
